import { Button } from './ui/button';
import { ListFilter} from 'lucide-react';

const AddFilter = () => {
    return (
        <Button size="icon">
            <ListFilter />

        </Button>
    );
}

export default AddFilter;
